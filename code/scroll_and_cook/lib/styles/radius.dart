import 'package:flutter/material.dart';

const BorderRadius kCircleRadius = BorderRadius.all(Radius.circular(100));
const BorderRadius kNormalRadius = BorderRadius.all(Radius.circular(20));
const BorderRadius kMinimalRadius = BorderRadius.all(Radius.circular(10));
const BorderRadius kMiniRadius = BorderRadius.all(Radius.circular(8));